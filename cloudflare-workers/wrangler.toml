name = "shaffercon-contact-form"
main = "contact-form.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Environment variables (set via Cloudflare dashboard or wrangler CLI)
# GITHUB_TOKEN will be set as a secret

[env.production]
routes = [
  { pattern = "api.shaffercon.com/contact", custom_domain = true }
]
